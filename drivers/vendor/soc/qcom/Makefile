# When adding new entries keep the list in alphabetical order
CFLAGS_scm.o :=$(call as-instr,.arch_extension sec,-DREQUIRES_SEC=1)

obj-y		+= debugpolicy/
obj-y		+= logcat/
obj-y		+= pmlog/
obj-y		+= sdlog/
obj-y		+= vcfg/
obj-y		+= vlog/
obj-y		+= dpn/
obj-y		+= imem_info/
obj-y		+= klog/
obj-y		+= subsys_restart/
obj-y	+= nubia_hw_version/
